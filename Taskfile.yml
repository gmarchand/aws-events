# https://taskfile.dev

version: '3'

dotenv: ['.env', '{{.ENV}}/.env.', '{{.HOME}}/.env']

tasks:
  default:
    
    cmds:
      - npm install -g youtube-playlist-markdown-cli
      - sh youtube_playlist_md.sh
      - python3 json_to_markdown.py ./FeedlySavedForLater1701416542820.json my-playlist-reinvent-2023.md
    silent: true

  awsevent:
    desc: AWS Event
    cmds:
      - ym -c UCdoadna9HFHsxXWhafhNvKw
